{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/ResendSellerLeadRequestRequest",
  "title": "",
  "required": [
    "status"
  ],
  "type": "object",
  "properties": {
    "status": {
      "type": "string",
      "description": "Seller Lead's status. Includes `accepted`, `connected` or `invited`.",
      "default": "accepted"
    }
  },
  "example": {
    "status": "accepted"
  }
}